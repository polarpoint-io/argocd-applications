chart:
  name: kargo-projects
  path: apps/controller/kargo-projects/non-prod
  url: git@github.com:polarpoint-io/argocd-applications.git
  targetRevision: main
  recurseDirectory: false
  namespace: kargo

labels:
  service: controller
  app: kargo

# Kargo Project/Warehouse/Stage/ProjectConfig CRs for all promotion pipelines.
# One subdirectory per project under templates/ - add new projects here as
# more apps get promotion pipelines. First pipeline: ciso-assistant (pilot).

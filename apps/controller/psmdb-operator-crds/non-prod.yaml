chart:
  name: psmdb-operator-crds
  url: https://percona.github.io/percona-helm-charts/
  targetRevision: 1.23.0
  namespace: psmdb-operator

labels:
  service: controller
  app: psmdb-operator

# Prerequisite for mongostate-crossplane's baremetal Composition, which
# provisions state via PerconaServerMongoDB CRs. Sync this before
# psmdb-operator and mongostate-crossplane.

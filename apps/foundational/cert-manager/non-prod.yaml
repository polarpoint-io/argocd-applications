chart:
  name: cert-manager
  url: https://charts.jetstack.io
  targetRevision: v1.21.1
  namespace: cert-manager

labels:
  service: foundational
  app: cert-manager

values: |
  crds:
    enabled: true

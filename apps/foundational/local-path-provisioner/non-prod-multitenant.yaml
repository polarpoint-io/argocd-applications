chart:
  name: local-path-provisioner
  path: apps/foundational/local-path-provisioner/non-prod-tooling
  url: git@github.com:polarpoint-io/argocd-applications.git
  targetRevision: main
  recurseDirectory: false
  namespace: local-path-storage

labels:
  service: foundational
  app: local-path-provisioner

values: |
  storageClass:
    create: true
    defaultClass: true
    name: local-path
